# Module three final project - User Forum

This project is a full stack web app similar to my module 2 final project with added functionality, design, and the use of react instead of pure JavaScript. Landing page displays recent forum posts and allows any user to view threads and replies. In order to reply to a thread or create a thread a user must either log in or register. Log in authenticated with JWT. Users can also view other user profiles and edit their own profiles.
